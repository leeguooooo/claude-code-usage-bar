{
  "name": "claude-statusbar",
  "owner": {
    "name": "leeguooooo",
    "email": "leeguooooo@gmail.com"
  },
  "metadata": {
    "description": "Self-hosted marketplace for the claude-statusbar plugin. Add with `/plugin marketplace add leeguooooo/claude-code-usage-bar`, then `/plugin install claude-statusbar`. The plugin wires the statusLine + slash commands; the render engine is the `cs` CLI from PyPI (`pip install claude-statusbar` or `uv tool install claude-statusbar`).",
    "version": "3.10.0"
  },
  "plugins": [
    {
      "name": "claude-statusbar",
      "source": "./",
      "description": "Lightweight Claude Code status-line monitor: 5h/7d rate-limit usage, reset timers, model + context window, prompt-cache countdown, optional session cost, project/branch (+ahead/behind), and an opt-in live-activity line (todos / active tool / subagents / session stats). 3 styles x 9 themes. Requires the `cs` CLI from PyPI.",
      "category": "monitoring",
      "tags": [
        "statusline",
        "monitoring",
        "rate-limit",
        "usage",
        "context-window",
        "prompt-cache",
        "todos",
        "subscriber"
      ]
    }
  ]
}
