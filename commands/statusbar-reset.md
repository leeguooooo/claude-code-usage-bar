---
description: Reset the status bar to the original classic + graphite defaults
allowed-tools: ["Bash"]
---

# Status Bar — reset to defaults

Run:

```bash
cs config set style classic
cs config set theme graphite
```

Then confirm with `cs config show` and tell the user the original look is restored.
